# 06 - 关键业务流程

## 6.1 视频播放流程

```mermaid
flowchart TD
    A[用户选择视频文件] --> B[NavHost 路由到 VideoPlayer]
    B --> C{判断文件扩展名}
    C -->|"m2ts/iso/mts/ts"| D[使用 VLC 引擎]
    C -->|"其他格式"| E{settings.defaultPlayer}
    E -->|"vlc"| D
    E -->|"exo 默认"| F[使用 ExoPlayer 引擎]
    D --> G[创建 MzVlcPlayer]
    F --> H[创建 MzExoPlayer]
    G --> I[选择 DataSource 工厂]
    H --> I
    I --> J{dataSourceType}
    J -->|SMB| K[SmbDataSourceFactory]
    J -->|FTP| L[FtpDataSourceFactory]
    J -->|WebDAV| M[WebDavDataSource]
    J -->|NFS| N[NfsDataSource]
    J -->|HTTP| O[DefaultHttpDataSource]
    J -->|Local| P[DefaultDataSource]
    K --> Q[播放器准备媒体源]
    L --> Q
    M --> Q
    N --> Q
    O --> Q
    P --> Q
    Q --> R[查询 media_history 断点]
    R --> S{有历史记录?}
    S -->|是| T[seekTo 历史位置]
    S -->|否| U[从头播放]
    T --> V[play]
    U --> V
    V --> W[加载同名弹幕 XML]
    W --> X[自动加载同名字幕]
    X --> Y[播放中]
    Y --> Z[定时保存进度到 media_history]
```

### 关键代码位置

| 步骤 | 文件 | 行 |
|------|------|-----|
| 路由参数解析 | [MzDKPlayerAPP.kt](../app/src/main/java/org/mz/mzdkplayer/ui/MzDKPlayerAPP.kt) | 392-421 |
| 引擎选择 | MzDKPlayerAPP.kt | 407-410 |
| 播放器创建 | [VideoPlayerScreen.kt](../app/src/main/java/org/mz/mzdkplayer/ui/videoplayer/VideoPlayerScreen.kt) | - |
| DataSource 工厂 | [FileMediaInfo.kt](../app/src/main/java/org/mz/mzdkplayer/tool/FileMediaInfo.kt) `builderPlayer()` | 198 |
| 历史查询 | MediaHistoryDao `getHistoryByUri()` | - |

## 6.2 弹幕加载与渲染流程

```mermaid
sequenceDiagram
    participant Player as VideoPlayerScreen
    participant Parser as DanmakuParser
    participant AKD as DanmakuPlayer (AKDanmaku)
    participant DS as DataSource

    Player->>DS: 获取同名 .xml 弹幕文件流
    DS-->>Player: InputStream?
    alt 文件存在
        Player->>Parser: getDanmakuXmlFromFile(stream)
        Parser->>Parser: DOM 解析 XML
        Parser->>Parser: 提取 <d p="time,type,color,...">text</d>
        Parser-->>Player: DanmakuResponse
        Player->>AKD: setDanmakuData(response.data)
        AKD->>AKD: 按 AKDanmakuConfig 渲染
        Note over AKD: 支持滚动/顶部/底部弹幕<br/>B站风格 RETAINER_BILIBILI
    else 文件不存在
        Note over Player: 无弹幕，正常播放
    end
```

### 弹幕文件匹配规则

- 视频文件名去扩展名 + `.xml`
- 示例：`video.mkv` → `video.xml`
- 本地文件检查存在性，网络协议直接尝试

### 弹幕配置

[DanmakuSettings.kt](../app/src/main/java/org/mz/mzdkplayer/data/model/DanmakuSettings.kt) + [DanmakuSettingsManager.kt](../app/src/main/java/org/mz/mzdkplayer/data/repository/DanmakuSettingsManager.kt) 管理弹幕样式：
- 字体大小、透明度、速度
- 显示区域比例（[DanmakuScreenRatio.kt](../app/src/main/java/org/mz/mzdkplayer/data/model/DanmakuScreenRatio.kt)）
- 弹幕类型开关（[DanmakuType.kt](../app/src/main/java/org/mz/mzdkplayer/data/model/DanmakuType.kt)）

## 6.3 TMDB 刮削流程

```mermaid
flowchart TD
    A[用户浏览文件列表] --> B[点击视频文件]
    B --> C{media_cache 有缓存?}
    C -->|是| D[直接显示详情]
    C -->|否| E[从文件名提取搜索关键词]
    E --> F[MediaInfoExtractorFormFileName]
    F --> G[TmdbRepository.searchMovies]
    G --> H{搜索结果?}
    H -->|有结果| I[取第一个结果]
    H -->|无结果| J[显示无信息]
    I --> K[TmdbRepository.getMovieDetails]
    K --> L[构建 MediaCacheEntity]
    L --> M[MediaDao.insertMedia 缓存]
    M --> N[显示详情页]
    N --> O[用户可编辑 TMDB 信息]
    O --> P[EditTMDBInfoScreen]
```

### 文件名解析

[MediaInfoExtractorFormFileName.kt](../app/src/main/java/org/mz/mzdkplayer/tool/MediaInfoExtractorFormFileName.kt) 从文件名提取：
- 标题（去除年份、分辨率、编码等标记）
- 年份
- 季号/集号（电视剧）

### 缓存策略

- 首次刮削后存入 `media_cache` 表
- `isDetailsLoaded` 标记是否已加载完整详情
- `groupKey` 用于搜索时分组（同一 TMDB ID 的不同版本只显示一条）

## 6.4 老人模式首页流程

```mermaid
flowchart TD
    A[App 启动] --> B{ModeManager.isElderMode}
    B -->|是| C[NavHost startDestination = ElderHomePage]
    B -->|否| D[NavHost startDestination = MainPage]
    C --> E[ElderHomeScreen]
    E --> F[加载 ElderModeConfig<br/>remember缓存避免重复加载]
    F --> G[HomeSlotRepository.allSlots Flow]
    G --> H[批量加载视频栏位播放历史缓存]
    H --> I[渲染顶部栏 ElderTopBar<br/>全部应用按钮]
    I --> J[渲染栏位网格]
    J --> K{slotType}
    K -->|folder| L[点击进入 ElderFolderPage]
    K -->|video| M[点击直接播放]
    K -->|app| N[AppLauncherHelper 启动 App]
    K -->|empty| O[弹出 SlotConfigDialog<br/>选择类型配置]
    I --> P[点击全部应用按钮]
    P --> Q[导航到 ElderAppListPage]
    Q --> R[加载已安装 App 列表]
    R --> S[网格展示所有 App]
    S --> T[点击启动 App]
    L --> U[ElderFolderScreen]
    U --> V[FolderVideoRepository 加载视频<br/>并行加载缩略图和历史]
    V --> W[渲染视频列表]
    W --> X[点击播放]
    O --> Y{选择类型}
    Y -->|app| Z[显示 App 列表供选择]
    Y -->|folder/video| AA[提示通过 Web 配置设置]
    Z --> AB[保存配置到 HomeSlotRepository]
    AB --> AC[关闭对话框<br/>栏位刷新显示]
```

### 空栏位配置流程

```mermaid
flowchart TD
    A[点击空栏位 + 号] --> B[弹出 SlotConfigDialog]
    B --> C[选择栏位类型]
    C --> D{类型}
    D -->|文件夹| E[提示需指定路径<br/>引导使用 Web 配置]
    D -->|视频| F[提示需指定路径<br/>引导使用 Web 配置]
    D -->|应用| G[加载已安装 App 列表]
    G --> H[选择 App]
    H --> I[HomeSlotRepository.updateSlot<br/>保存配置]
    I --> J[关闭对话框<br/>栏位刷新显示]
```

### 应用列表页与包变更监听

[ElderAppListScreen.kt](../app/src/main/java/org/mz/mzdkplayer/ui/elder/ElderAppListScreen.kt) 提供 5 列网格展示所有已安装 App：

| 特性 | 说明 |
|------|------|
| 入口 | 首页顶部"全部应用"按钮 |
| 列表来源 | `AppLauncherHelper.getLaunchableApps()`，排除自身 |
| 图标渲染 | `drawableToImageBitmap()` 支持 AdaptiveIcon/VectorDrawable |
| 点击行为 | 直接启动 App |
| 实时刷新 | `DisposableEffect` 注册 `PACKAGE_ADDED/REMOVED/REPLACED` 广播 |

#### 包变更监听流程

```mermaid
flowchart TD
    A[系统广播] --> B{Intent.action}
    B -->|PACKAGE_ADDED| C[刷新 App 列表]
    B -->|PACKAGE_REMOVED| D[HomeSlotRepository.cleanupUninstalledAppSlots]
    B -->|PACKAGE_REPLACED| C
    D --> E[遍历 app 类型栏位]
    E --> F{包已卸载?}
    F -->|是| G[重置为 empty 栏位]
    F -->|否| H[保留原配置]
    G --> I[Flow 自动推送更新]
    C --> I
    I --> J[UI 刷新显示]
```

#### 关键代码位置

| 步骤 | 文件 | 行 |
|------|------|-----|
| 广播注册 | [ElderAppListScreen.kt](../app/src/main/java/org/mz/mzdkplayer/ui/elder/ElderAppListScreen.kt) | DisposableEffect |
| 清理失效栏位 | [HomeSlotRepository.kt](../app/src/main/java/org/mz/mzdkplayer/data/repository/HomeSlotRepository.kt) | `cleanupUninstalledAppSlots()` |
| 图标转换 | [AppLauncherHelper.kt](../app/src/main/java/org/mz/mzdkplayer/tool/AppLauncherHelper.kt) | `drawableToImageBitmap()` |

### 返回键行为

老人模式首页返回键采用**双击确认退出**机制：
- 第一次按下：显示"再按一次返回键退出"提示（2秒内有效）
- 第二次按下（2秒内）：退出应用
- 超过2秒：重置状态，需重新双击

### 模式切换流程

```mermaid
flowchart LR
    A[老人模式] -->|长按下键 5 秒| B[PIN 输入弹窗]
    B --> C{PIN 已设置?}
    C -->|否 首次| D[引导设置 PIN]
    C -->|是| E[验证 PIN]
    D --> F[切换到标准模式]
    E -->|正确| G[Activity.recreate<br/>保留状态重建]
    E -->|错误| H[提示错误]
    F --> I[标准模式]
    G --> I
    I -->|设置页底部<br/>切换为老人模式| A
```

## 6.5 播放历史与断点续播

```mermaid
sequenceDiagram
    participant UI as 播放器
    participant Hist as MediaHistoryRepository
    participant DAO as MediaHistoryDao

    Note over UI: 播放开始
    UI->>Hist: getHistoryByUri(mediaUri)
    alt 有历史
        Hist->>DAO: SELECT WHERE mediaUri = ?
        DAO-->>Hist: MediaHistoryEntity
        Hist-->>UI: 播放位置 + 时长
        UI->>UI: seekTo(playbackPosition)
    end

    Note over UI: 播放中（定时）
    loop 每 N 秒
        UI->>Hist: insertHistory(MediaHistoryEntity)
        Hist->>DAO: INSERT OR REPLACE
    end

    Note over UI: 退出播放
    UI->>Hist: insertHistory(最终位置)
```

### 老人模式续播差异

| 行为 | 标准模式 | 老人模式 |
|------|---------|---------|
| 续播确认 | 弹窗询问 | 自动续播，无弹窗（`autoResume=true`） |
| 播放结束 | 停留在播放器 | 根据 `stayOnPageAfterEnd` 配置：<br/>- `true`：停留显示控制栏<br/>- `false`：自动返回上一页 |

### 老人模式播放器控制栏

[ElderPlayerOverlay.kt](../app/src/main/java/org/mz/mzdkplayer/ui/elder/ElderPlayerOverlay.kt) 提供简化控制栏：

| 特性 | 说明 |
|------|------|
| 按钮 | 仅 3 个大按钮：后退、播放/暂停、前进 |
| 进度条 | 6dp 加粗进度条，支持点击/拖动跳转 |
| 时间显示 | 大字号（22sp） |
| 高级控制 | 根据配置隐藏弹幕、网速、轨道等按钮 |

进度条交互：
- 点击任意位置跳转到对应时间点
- 拖动实时预览进度，松手后跳转

## 6.6 Web 配置服务流程

```mermaid
sequenceDiagram
    participant Family as 家属浏览器
    participant Server as WebConfigServer:18080
    participant Repo as 各 Repository
    participant DB as Room/SharedPreferences

    Note over Family: 局域网访问 http://TV_IP:18080
    Family->>Server: GET / (配置页面)
    Server-->>Family: HTML + JS

    Family->>Server: GET /api/slots
    Server->>Repo: HomeSlotRepository.getAllSlotsSync()
    Repo->>DB: SELECT * FROM home_slots
    DB-->>Repo: List<HomeSlotEntity>
    Repo-->>Server: List
    Server-->>Family: JSON {ok:true, data:[...]}

    Family->>Server: PUT /api/slots/reorder
    Server->>Repo: HomeSlotRepository.reorderSlots(ids)
    Repo->>DB: UPDATE sortOrder
    Server-->>Family: JSON {ok:true}

    Note over Family: TV 端通过 Flow 自动刷新
```

### 启动条件

在 [MainActivity.onCreate](../app/src/main/java/org/mz/mzdkplayer/MainActivity.kt#L103)：

```kotlin
(application as MzDkPlayerApplication).startWebConfigServerIfNeeded()
```

仅当 `SettingsRepository.webConfigEnabled == true` 时启动。

## 6.7 外部视频 Intent 处理

```mermaid
flowchart TD
    A[外部 App 分享视频] --> B[Android 系统 Intent]
    B --> C[MainActivity.onNewIntent]
    C --> D[handleIntent]
    D --> E{MIME 类型}
    E -->|video/*| F[设置 externalVideoUri]
    E -->|其他| G[忽略]
    F --> H[LaunchedEffect 检测]
    H --> I[NavHost 导航到 VideoPlayer]
```

### 支持的 Intent

[AndroidManifest.xml](../app/src/main/AndroidManifest.xml) 声明了两类 intent-filter：

1. **MIME 类型**：`video/*`、`application/x-mpegurl` 等
2. **文件扩展名**：`.mp4`、`.mkv`、`.avi`、`.mov`、`.flv`、`.m3u8`、`.m2ts`

### 已知限制

[MainActivity.kt:117](../app/src/main/java/org/mz/mzdkplayer/MainActivity.kt#L117) 注释指出：`externalVideoUri` 是 Activity 字段，`onNewIntent` 更新后不会触发已初始化的 Compose 重组。对于简单场景（只启动一次播放）已足够。

## 6.8 音频播放流程

```mermaid
flowchart TD
    A[选择音频文件] --> B[NavHost 路由 AudioPlayer]
    B --> C[从 MzDkPlayerApplication.getStringList<br/>获取播放列表]
    C --> D[创建 ExoPlayer]
    D --> E[jaudiotagger 读取元数据]
    E --> F{audio_cache 有缓存?}
    F -->|是| G[直接使用缓存]
    F -->|否| H[读取标签/封面/歌词]
    H --> I[缓存到 audio_cache]
    G --> J[显示播放界面]
    I --> J
    J --> K[歌词滚动/封面显示]
```

### 播放列表传递

⚠️ 音频播放列表通过 `MzDkPlayerApplication.setStringList("audio_playlist", list)` 全局静态 Map 传递，进程回收后丢失。

## 6.9 SMB 自动扫描与一键连接流程

```mermaid
flowchart TD
    A[用户点击扫描按钮] --> B[SmbScanViewModel.startScan]
    B --> C[SmbDiscoveryScanner 三阶段扫描]
    C --> D[阶段1: mDNS 发现 _smb._tcp]
    C --> E[阶段2: 子网 445 端口并发扫描]
    D --> F[合并结果到 StateFlow]
    E --> F
    F --> G[阶段3: Guest 认证探测]
    G --> H[更新 requiresAuth + hostname]
    H --> I[智能排序]
    I --> J[展示扫描结果列表]
    J --> K{用户点击主机}
    K -->|匹配已保存连接| L[直接进入 SMBFileListScreen]
    K -->|新主机| M[跳转 SMBConScreen 预填 IP]
    M --> N[用户输入凭据]
    N --> O[testConnectSMB]
    O --> P[SmbConnectionManager.getOrConnect]
    P --> Q[连接成功 → 文件列表]
```

### 三阶段扫描策略

[SmbDiscoveryScanner.kt](../app/src/main/java/org/mz/mzdkplayer/tool/SmbDiscoveryScanner.kt) 采用三阶段策略，结果通过 `StateFlow` 增量推送：

| 阶段 | 方法 | 说明 | 超时 |
|------|------|------|------|
| 1. mDNS 发现 | `discoverViaMdns()` | NsdManager 搜索 `_smb._tcp`，快速但非所有 SMB 服务都注册 | 3s |
| 2. 子网端口扫描 | `scanSubnetPort()` | 并发扫描 254 个 IP 的 445 端口，全面覆盖 | 800ms/IP |
| 3. Guest 认证探测 | `probeSmbAuth()` | 尝试 guest 认证，判断是否需要密码 | 3s/主机 |

阶段 1 和 2 并行执行，阶段 3 在前两阶段完成后对发现的主机并发探测。

### 智能排序策略

[SmbScanViewModel.kt](../app/src/main/java/org/mz/mzdkplayer/ui/screen/vm/SmbScanViewModel.kt) 的 `getSortedHosts()` 按以下优先级排序：

1. **已保存连接优先** — 匹配 `SMBConnectionRepository` 中的记录
2. **免认证优先** — `requiresAuth == false` 的主机排前
3. **响应时间短优先** — `responseTimeMs` 升序
4. **名称排序** — hostname 字母序兜底

### 一键连接逻辑

[SMBConListScreen.kt](../app/src/main/java/org/mz/mzdkplayer/ui/screen/smbfile/SMBConListScreen.kt) 中点击发现的主机：

| 场景 | 行为 |
|------|------|
| 匹配已保存连接 | 直接导航到 `SMBFileListScreen`，URI 含账号密码 |
| 新主机 | 导航到 `SMBConScreen?ip={ip}`，预填 IP，用户手动输入凭据 |

### 全局连接管理与自动重连

[SmbConnectionManager.kt](../app/src/main/java/org/mz/mzdkplayer/tool/SmbConnectionManager.kt) 是单例，`SMBConViewModel` 的连接管理已委托给它：

| 能力 | 说明 |
|------|------|
| 连接复用 | `getOrConnect(id, ...)` 按 `host:shareName` 复用已有连接 |
| 指数退避重连 | 1s → 2s → 4s → 8s → 16s，最多 5 次 |
| 后台健康巡检 | 每 30s 检查空闲连接（空闲 >60s 时触发），通过 `share.list("\\")` 探活 |
| 状态推送 | `StateFlow<Map<String, ConnectionState>>` 供 UI 观察 |

`SMBConViewModel` 的 `connectToSMB` / `testConnectSMB` 调用 `getOrConnect()`，`disconnectSMB` 调用 `disconnect()`，`isConnected()` 查询 `getState()`。`listSMBFiles` 在 share 为 null 时自动通过 Manager 重连。
